# จับมือทำ — สร้าง Bucket ใน Supabase (เก็บรูปทรัพย์)

ทำตามทีละขั้น ไม่ต้องงง

---

## ขั้นที่ 1 — เปิด Supabase แล้วเข้า Storage

1. เปิดเบราว์เซอร์ (Chrome / Edge ฯลฯ)
2. ไปที่ลิงก์นี้ (ถ้ายังไม่ล็อกอิน Supabase จะให้ล็อกอินก่อน):

   **https://supabase.com/dashboard**

3. ล็อกอินแล้ว จะเห็นรายการโปรเจกต์ — **คลิกที่ชื่อโปรเจกต์ของคุณ** (โปรเจกต์ที่ใช้กับเว็บ Prime Axis Estate)
4. ทางซ้ายมือจะมีเมนูหลายอัน — **คลิกคำว่า "Storage"** (ไอคอนเป็นรูปโฟลเดอร์)

   ถ้าไม่เห็นเมนูซ้าย ให้ลองคลิก **☰** (แฮมเบอร์เกอร์) ด้านบนซ้ายก่อน แล้วจะโผล่เมนู

---

## ขั้นที่ 2 — สร้าง Bucket ใหม่

5. พอเข้า Storage แล้ว ด้านบนจะมีปุ่ม **"New bucket"** — **คลิกปุ่มนี้**
6. จะโผล่หน้าต่าง (หรือฟอร์ม) ให้ตั้งค่า bucket:

   | ช่องที่เห็น | ทำอย่างนี้ |
   |------------|-----------|
   | **Name** | พิมพ์ตรงนี้ว่า **`property-images`** (ภาษาอังกฤษ มีขีดกลาง ไม่มีเว้นวรรค) |
   | **Public bucket** | **ติ๊กถูก** (เปิดให้เป็น Public) — เพื่อให้ลิงก์รูปใช้โชว์บนเว็บได้ |

7. **อย่า** ติ๊ก "Allowed MIME types" หรือ "File size limit" ถ้าไม่แน่ใจ (ข้ามได้)
8. กดปุ่ม **"Create bucket"** หรือ **"Save"** ด้านล่าง

---

## ขั้นที่ 3 — เช็กว่าสร้างแล้ว

9. กลับมาหน้า Storage จะเห็นรายการ bucket — ต้องมีชื่อ **`property-images`** อยู่
10. เสร็จขั้นตอนสร้าง bucket แล้ว

---

## ลิงก์ตรง (ถ้าอยากเปิดเร็ว)

- เปิด Dashboard โปรเจกต์: **https://supabase.com/dashboard**
- หลังเลือกโปรเจกต์แล้ว ไป Storage โดยตรง (แทนชื่อโปรเจกต์ด้วยของจริง):  
  **https://supabase.com/dashboard/project/liyjjuqzsvgwwopvcdko/storage/buckets**

  (ถ้าโปรเจกต์คุณไม่ใช่ `liyjjuqzsvgwwopvcdko` ให้ไปที่ Dashboard → คลิกโปรเจกต์ → คลิก Storage แทน)

---

## ต่อไปทำอะไร

สร้าง bucket เสร็จแล้ว ต้องไปตั้งค่า **Environment Variables** ต่อ (ดูในไฟล์ **ตั้งค่า-Supabase-Storage.md** ขั้นที่ 2) แล้วถึงจะอัปโหลดรูปไปที่ bucket นี้ได้
