# ข้อ 2 ทำยังไง (เชื่อมโปรเจกต์กับ GitHub) — ทำทีละขั้น

## ก่อนเริ่ม

- สร้าง **repo ว่าง** บน GitHub ไว้แล้ว (ข้อ 1)
- ต้องมี **Git** ในเครื่อง  
  - ถ้ายังไม่มี: โหลดที่ https://git-scm.com/download/win แล้วติดตั้ง (กด Next ไปเรื่อยๆ ได้)

---

## วิธีที่ 1: ใช้ไฟล์กดรัน (ง่ายสุด)

### ขั้นที่ 1 — ใส่ลิงก์ repo ของคุณ

1. เปิดโฟลเดอร์โปรเจกต์ (ที่เก็บไฟล์เว็บ)
2. หาไฟล์ชื่อ **`push-to-github.bat`**
3. **คลิกขวา** ที่ไฟล์นั้น → เลือก **แก้ไข** หรือ **Open with Notepad**
4. หาบรรทัดที่เขียนประมาณนี้:
   ```bat
   set GITHUB_URL=https://github.com/ใส่ชื่อuser/ใส่ชื่อrepo.git
   ```
5. **ลบ** ส่วน `ใส่ชื่อuser` กับ `ใส่ชื่อrepo` ออก แล้ว**แทนที่**ด้วยของจริง  
   - ตัวอย่าง: ถ้า username คือ `john` และชื่อ repo คือ `prime-axis-estate`  
   - ให้กลายเป็น:
   ```bat
   set GITHUB_URL=https://github.com/john/prime-axis-estate.git
   ```
6. **บันทึกไฟล์** (Ctrl+S) แล้วปิด Notepad

### ขั้นที่ 2 — กดรัน

1. ดับเบิลคลิกที่ **`push-to-github.bat`**
2. หน้าต่างสีดำจะโผล่ รอจนมีคำว่า “ส่งขึ้น GitHub เรียบร้อย” หรือมีข้อความถามอะไร
3. **ถ้ามันถามชื่อ user / รหัสผ่าน:**
   - User = **ชื่อ user GitHub** ของคุณ
   - Password = **ไม่ใช่รหัสผ่านล็อกอิน** แต่ให้ใช้ **Personal Access Token**  
     - ไปที่ github.com → ล็อกอิน → กดรูปโปรไฟล์มุมขวา → **Settings**  
     - ซ้ายล่าง **Developer settings** → **Personal access tokens** → **Tokens (classic)**  
     - **Generate new token (classic)** → ตั้งชื่อ เช่น “vercel” → เลือกอายุ → ติ๊ก **repo**  
     - กด Generate → ** copy โทเคนไว้** แล้วไปวางในช่อง Password ตอนที่ bat ถาม

ทำแค่นี้โปรเจกต์จะถูก push ขึ้น GitHub ตาม repo ที่คุณตั้งไว้

---

## วิธีที่ 2: ใช้คำสั่งเอง (ถ้าชอบใช้ Terminal)

เปิด **PowerShell** หรือ **Command Prompt** ในโฟลเดอร์โปรเจกต์ แล้วรันทีละบรรทัด (แทนที่ `john` กับ `prime-axis-estate` เป็นของจริง):

```powershell
git init
git remote add origin https://github.com/john/prime-axis-estate.git
git add .
git commit -m "Initial: PRIME AXIS ESTATE"
git branch -M main
git push -u origin main
```

ถ้าถามล็อกอิน ใช้ **Username + Personal Access Token** (เหมือนวิธีที่ 1)

---

## สรุปหนึ่งบรรทัด

- **วิธีที่ 1**: แก้ลิงก์ใน **`push-to-github.bat`** → ดับเบิลคลิกรัน → ถามล็อกอินก็ใส่ user + Token  
- **วิธีที่ 2**: เปิด Terminal ในโฟลเดอร์โปรเจกต์ → วางคำสั่งด้านบนทีละบรรทัด (แก้ชื่อ repo ให้ตรงของจริง)

เสร็จข้อ 2 แล้ว ไปทำ **ข้อ 3 (Deploy บน Vercel)** ต่อได้เลย
