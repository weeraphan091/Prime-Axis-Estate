# ทำอะไรต่อ — ลำดับขั้นตอน

ระบบฝั่งโค้ดตั้งค่าไว้แล้ว คุณทำแค่ 3 อย่างนี้ตามลำดับ:

---

## ข้อ 1 — สร้าง Bucket ใน Supabase (ยังไม่ได้สร้างให้ทำก่อน)

1. เปิด **https://supabase.com/dashboard** แล้วเลือกโปรเจกต์
2. คลิก **Storage** (เมนูซ้าย)
3. คลิก **New bucket**
4. ชื่อ bucket ใส่: **`property-images`**
5. ติ๊ก **Public bucket**
6. กด **Create bucket**

(มีรายละเอียดในไฟล์ **จับมือทำ-สร้าง-Bucket-Supabase.md**)

---

## ข้อ 2 — ใส่ตัวแปรใน .env (ในเครื่อง)

1. เปิดโฟลเดอร์โปรเจกต์ → เปิดไฟล์ **`.env`** (ถ้าไม่มี ให้ copy จาก `.env.example` แล้วเปลี่ยนชื่อเป็น `.env`)
2. เพิ่มหรือแก้ให้มี 3 บรรทัดนี้ (ใส่รหัสผ่าน DB จริงแทนที่ `รหัสผ่านDBจริง`):

```
NEXT_PUBLIC_SUPABASE_URL=https://liyjjuqzsvgwwopvcdko.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ค่าที่คุณ copy ไว้ (sb_secret_... หรือ service_role key)
DATABASE_URL="postgresql://postgres.liyjjuqzsvgwwopvcdko:รหัสผ่านDBจริง@aws-1-ap-south-1.pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true"
```

3. Save ไฟล์

---

## ข้อ 3 — ใส่ตัวแปรบน Vercel (ถ้า deploy บน Vercel)

1. เปิด **https://vercel.com** → เลือกโปรเจกต์ Prime Axis Estate
2. ไปที่ **Settings** → **Environment Variables**
3. เพิ่ม 3 ตัว (ชื่อและค่าให้ตรงกับใน .env):
   - **NEXT_PUBLIC_SUPABASE_URL**
   - **SUPABASE_SERVICE_ROLE_KEY**
   - **DATABASE_URL** (ใช้รหัสผ่าน DB จริง และต่อท้าย `&pgbouncer=true`)
4. กด **Save**
5. ไปที่ **Deployments** → กด **Redeploy** ที่ deployment ล่าสุด

---

## เสร็จแล้วตรวจสอบยังไง

- **ในเครื่อง:** รัน `npm run dev` แล้วลองฝากทรัพย์หรือแอดมินอัปโหลดรูป — ถ้าสำเร็จ รูปจะไปโผล่ใน Supabase → Storage → bucket `property-images`
- **บน Vercel:** หลัง Redeploy แล้วลองอัปโหลดรูปจากเว็บจริง — ถ้าไม่มี error แปลว่าพร้อมใช้
