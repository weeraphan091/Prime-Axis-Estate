# สร้างบัญชีแอดมินคนแรก (หลังบ้าน)

ถ้าเข้าหลังบ้านแล้วขึ้น **「อีเมลหรือรหัสผ่านไม่ถูกต้อง」** แปลว่าในฐานข้อมูลยังไม่มีบัญชีแอดมิน ต้องสร้างก่อนหนึ่งครั้ง

---

## วิธีที่ 1: รัน seed จากเครื่องคุณ (แนะนำ)

1. เปิดไฟล์ **`.env`** ในโฟลเดอร์โปรเจกต์  
   - ให้ **`DATABASE_URL`** ชี้ไปที่ฐานข้อมูล **ตัวเดียวกับที่ใช้บน Vercel** (Supabase ตัวเดียวกัน)

2. เปิด Command Prompt หรือ PowerShell แล้วรัน:
   ```bash
   cd โฟลเดอร์โปรเจกต์อสังหา
   npm run db:seed
   ```

3. ถ้ารันสำเร็จจะเห็นข้อความประมาณ:
   - `Seeded admin: Kiranat56201@gmail.com ...`

4. ลองเข้าหลังบ้านอีกครั้งที่ **`/admin/login`**  
   - **อีเมล:** `Kiranat56201@gmail.com`  
   - **รหัสผ่าน:** `Venus0979961789`  
   - (ถ้าใน `.env` ตั้ง `ADMIN_INITIAL_PASSWORD=รหัสอื่น` ไว้ ให้ใช้รหัสนั้นแทน)

---

## วิธีที่ 2: สร้างแอดมินใน Supabase ด้วย SQL

ถ้ารัน seed จากเครื่องไม่ได้ (เช่น ไม่มี Node / ไม่ได้ตั้ง DATABASE_URL) ให้สร้างบัญชีแอดมินใน Supabase โดยตรง:

1. สร้างรหัสผ่านแฮชด้วย Node (รันครั้งเดียวในโฟลเดอร์โปรเจกต์):
   ```bash
   node -e "const bcrypt=require('bcryptjs'); bcrypt.hash('Venus0979961789',10).then(h=>console.log(h))"
   ```
   จะได้ข้อความยาวๆ ขึ้นหนึ่งบรรทัด (เช่น `$2a$10$...`) → ** copy ไว้**

2. เปิด **Supabase → SQL Editor** แล้วรัน (แทน `ใส่แฮชจากขั้นตอน1` ด้วยค่าที่ copy):
   ```sql
   INSERT INTO "AdminUser" ("id", "email", "passwordHash", "name", "role", "isActive", "createdAt", "updatedAt")
   VALUES (
     gen_random_uuid()::text,
     'kiranat56201@gmail.com',
     'ใส่แฮชจากขั้นตอน1',
     'ผู้ดูแลระบบหลัก',
     'admin',
     true,
     to_char(now(), 'YYYY-MM-DD"T"HH24:MI:SS'),
     to_char(now(), 'YYYY-MM-DD"T"HH24:MI:SS')
   );
   ```

3. เข้าหลังบ้านด้วยอีเมล **`Kiranat56201@gmail.com`** รหัส **`Venus0979961789`**

---

## หมายเหตุ

- อีเมลในระบบเป็น **ตัวเล็กทั้งหมด** (`kiranat56201@gmail.com`) ใส่แบบมีตัวใหญ่ก็ได้ ระบบจะแปลงให้
- หลังสร้างบัญชีแล้วสามารถสร้างบัญชีพนักงานเพิ่มจากเมนู **บัญชีผู้ใช้** ในหลังบ้าน (เมื่อล็อกอินเป็น admin)
