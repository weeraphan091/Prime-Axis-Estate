# ติดตั้ง Git ก่อน (ถึงจะส่งโปรเจกต์ขึ้น GitHub ได้)

## ขั้นตอน

### 1. โหลด Git

1. เปิดเบราว์เซอร์ ไปที่: **https://git-scm.com/download/win**
2. จะโหลดไฟล์ **Git for Windows** ให้เอง (ชื่อประมาณ `Git-2.43.0-64-bit.exe`)
3. รอโหลดเสร็จ

### 2. ติดตั้ง

1. ดับเบิลคลิกไฟล์ที่โหลดมา
2. หน้าต่างติดตั้งโผลมา → กด **Next** ไปเรื่อยๆ (ค่า default ได้หมด)
3. จนถึง **Install** → กด Install
4. ติดตั้งเสร็จ → กด **Finish**

### 3. รัน push อีกครั้ง

1. **ปิด** หน้าต่าง Command Prompt ที่ขึ้น error ไว้ (ถ้ายังเปิดอยู่)
2. ดับเบิลคลิก **`push-to-github.bat`** อีกครั้ง
3. ตอนนี้คำสั่ง `git` ควรใช้ได้แล้ว

ถ้ายังขึ้น `'git' is not recognized` อีก:
- **รีสตาร์ทเครื่อง** แล้วลองดับเบิลคลิก `push-to-github.bat` อีกครั้ง

---

## สรุป

- **สาเหตุ:** ยังไม่ได้ติดตั้ง Git
- **แก้:** โหลดที่ https://git-scm.com/download/win → ติดตั้ง → ปิด CMD แล้วรัน `push-to-github.bat` ใหม่ (หรือรีสตาร์ทเครื่องก่อน)
