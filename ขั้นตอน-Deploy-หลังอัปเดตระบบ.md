# ขั้นตอน Deploy หลังอัปเดตระบบ (ลีด, พนักงานขาย, สถานะรายการ)

ระบบเพิ่ม: **ลีดใน DB**, **พนักงานขาย (Agent)**, **สถานะรายการ (แบบร่าง/เผยแพร่/ขายแล้ว)**, **แดชบอร์ด**, **Export Excel**

---

## 1. รัน SQL ใน Supabase (ครั้งเดียว)

ถ้าฐานข้อมูลมีตาราง **Property** อยู่แล้ว (เคยรัน `supabase-create-tables.sql` มาก่อน):

1. เปิด **Supabase SQL Editor**:  
   https://supabase.com/dashboard/project/liyjjuqzsvgwwopvcdko/sql/new
2. เปิดไฟล์ **`supabase-migration-leads-agents-status.sql`** ในโปรเจกต์
3. Copy เนื้อหาทั้งหมด → วางใน SQL Editor → กด **Run**

จะได้ตาราง **Agent**, **Lead** และคอลัมน์ **status**, **agentId** ใน Property

---

## 2. Push โค้ดขึ้น Git

```bash
git add -A
git commit -m "Add leads, agents, listing status, dashboard, export"
git push origin main
```

(หรือใช้ Cursor/IDE push ขึ้น GitHub ตามปกติ)

---

## 3. ตรวจสอบ Vercel

- โปรเจกต์ผูกกับ GitHub แล้ว → Vercel จะ **build และ deploy อัตโนมัติ**
- ตรวจสอบว่าใน Vercel → **Settings → Environment Variables** มี **DATABASE_URL** (แบบ Pooler + `pgbouncer=true`)

---

## 4. ทดสอบหลัง Deploy

- **หลังบ้าน** → แดชบอร์ด (ตัวเลขรายการ/ลีด/พนักงาน)
- **พนักงานขาย** → เพิ่มพนักงาน แล้วไปลงลิสใหม่ เลือกสถานะ + พนักงานรับผิดชอบ
- **ลีด** → กด "สนใจทรัพย์นี้" ในเว็บหลัก ส่งฟอร์ม → ดูในหลังบ้านว่ามีลีดใหม่ และแจ้งเตือนเข้า Telegram/Line ตามที่ตั้งไว้
- **Export Excel** → แดชบอร์ดหรือหน้ารายการ/ลีด กด Export รายการทรัพย์ / ลีด เปิดใน Excel ได้

---

## สรุปฟีเจอร์ที่เพิ่ม

| ฟีเจอร์ | รายละเอียด |
|--------|-------------|
| **ลีดใน DB** | กด "สนใจทรัพย์นี้" → บันทึกในตาราง Lead + ส่งแจ้งเตือน Line/Telegram เหมือนเดิม |
| **จัดการลีด** | หลังบ้าน → ลีด ดู/อัปเดตสถานะ (ใหม่/ติดต่อแล้ว/นัดชมแล้ว/ปิดการขาย) และผูกพนักงานรับผิดชอบ |
| **พนักงานขาย** | หลังบ้าน → พนักงานขาย เพิ่ม/แก้ไข โปรไฟล์ (ชื่อ, โทร, อีเมล, Line) |
| **สถานะรายการ** | ลงลิส/แก้ไขรายการ เลือกได้: แบบร่าง (ไม่โชว์ในเว็บ), เผยแพร่, ขายแล้ว/เช่าแล้ว |
| **ผูกรายการกับพนักงาน** | เลือกพนักงานรับผิดชอบต่อรายการได้ในฟอร์มลงลิส/แก้ไข |
| **แดชบอร์ด** | หลังบ้านหน้าแรก แสดงจำนวนรายการ, ลีด (รวมสัปดาห์นี้), พนักงาน และลิงก์ Export |
| **Export Excel** | ดาวน์โหลด CSV รายการทรัพย์ และ ลีด เปิดใน Excel ได้ |
