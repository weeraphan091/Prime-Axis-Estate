# รันใช้ได้จริง — สิ่งที่ทำแล้ว vs สิ่งที่ควรทำต่อ

## ✅ ตอนนี้รันใช้ได้แล้ว (ในเครื่อง)

### ครั้งแรกที่เปิดโปรเจกต์
1. เปิด Terminal ในโฟลเดอร์โปรเจกต์
2. รันตามลำดับ:
   ```bash
   npm install
   npx prisma generate
   npx prisma db push
   npm run db:seed
   npm run dev
   ```
3. เปิดเบราว์เซอร์ที่ **http://localhost:3000**

### ครั้งถัดไป (เปิดโปรเจกต์ซ้ำ)
- รันแค่ `npm run dev` (หรือดับเบิลคลิก `start.bat` ถ้ามี)

### ตั้งค่าที่ควรทำก่อนใช้จริง
- สร้างไฟล์ **`.env`** ในโฟลเดอร์โปรเจกต์ ( copy จาก `.env.example` )
  - **ADMIN_PASSWORD** = รหัสผ่านเข้าหลังบ้าน (เปลี่ยนจาก admin123)
  - **SESSION_SECRET** = ค่าลับสำหรับ session สมาชิก (สร้างค่าสุ่มยาวๆ เช่น `openssl rand -base64 32`)
  - **การแจ้งเตือนเข้า Line:** ใช้ **LINE Messaging API** (แนะนำ) — ตั้งค่า `LINE_CHANNEL_ACCESS_TOKEN` และ `LINE_AGENT_USER_ID` ใน `.env` (ดูรายละเอียดใน `.env.example`). Line Notify ยุติบริการ 31 มี.ค. 2568
- หลังบ้าน → **ข้อมูลติดต่อ** → แก้เบอร์โทร อีเมล Line ID ฯลฯ ให้เป็นของจริง
- **สมาชิกทดสอบหลัง seed:** อีเมล `demo@example.com` รหัสผ่าน `demo123` (ใช้ทดสอบล็อกอินและฝากขาย/เช่า)

---

## 📋 สรุปฟีเจอร์ที่พร้อมใช้

| ฟีเจอร์ | สถานะ |
|--------|--------|
| ดูรายการทรัพย์ ค้นหา กรอง | ✅ ใช้ได้ |
| หน้ารายละเอียดทรัพย์ + แกลเลอรี่รูป | ✅ ใช้ได้ |
| สนใจทรัพย์นี้ → กรอกฟอร์ม → ส่งเข้า Line (Messaging API หรือ Line Notify) | ✅ ใช้ได้ |
| ฝากขาย/เช่า → ต้องล็อกอิน/สมัครก่อน → กรอกฟอร์ม | ✅ ใช้ได้ (บันทึกลง DB + อัปโหลดรูปได้) |
| เข้าสู่ระบบ / สมัครสมาชิก | ✅ ใช้ได้ (API จริง เก็บใน DB + session cookie) |
| หลังบ้าน: ล็อกอิน แก้รายการ ลงลิสใหม่ แก้ข้อมูลติดต่อ | ✅ ใช้ได้ |
| ข้อมูลติดต่อแก้หลังบ้านแล้วเปลี่ยนทั้งเว็บ | ✅ ใช้ได้ |

---

## ⚠️ สิ่งที่ควรทำถ้าจะใช้จริง (รับลูกค้าจริง / ขึ้นเซิร์ฟเวอร์)

### 1. เผยแพร่เว็บ (Deploy)
- **Vercel** (แนะนำ): สร้างบัญชี → เชื่อม Git → Deploy
  - หมายเหตุ: Vercel เป็น serverless อาจต้องเปลี่ยนจาก SQLite เป็น **PostgreSQL** (เช่น Vercel Postgres, Supabase) เพื่อให้ข้อมูลคงอยู่
- หรือเช่า **VPS / Hosting** ที่รัน Node.js ได้ แล้วรัน `npm run build` + `npm run start`

### 2. ฐานข้อมูลบนเซิร์ฟเวอร์
- ตอนนี้ใช้ **SQLite** (ไฟล์ในเครื่อง) เหมาะกับรันในเครื่อง
- ถ้า Deploy: ใช้ **PostgreSQL** หรือ **MySQL** แล้วแก้ `prisma/schema.prisma` ให้ชี้ไปที่ DB จริง

### 3. ระบบสมาชิก (ทำแล้ว)
- ล็อกอิน/สมัครใช้ **API จริง** เก็บ user ใน DB (ตาราง User) + **session cookie** (JWT ใน cookie)
- ฝากขาย/เช่าต้องล็อกอินก่อน → บันทึกลง DB ทันที (API `/api/owner-listings`)

### 4. รายการที่ลูกค้าฝาก (ทำแล้ว)
- ฟอร์มฝากขาย/เช่าส่งไป **POST /api/owner-listings** → บันทึกลงตาราง Property (isOwnerListing=true)
- รูปอัปโหลดผ่าน **POST /api/upload** → เก็บใน `public/uploads` แล้วใส่ URL ในรายการ

### 5. รูปภาพ
- ตอนนี้รูปจากฟอร์มฝากขาย/เช่า → อัปโหลดไป **public/uploads** แล้วเก็บ **URL** ใน DB
- ถ้า Deploy บน Vercel (serverless): โฟลเดอร์ public ไม่ persist แนะนำใช้ **Cloud Storage** (S3, Cloudinary) แทน

### 6. การแจ้งเตือนเข้า Line
- **LINE Messaging API** (แนะนำ): ตั้งค่า `LINE_CHANNEL_ACCESS_TOKEN` + `LINE_AGENT_USER_ID` ใน `.env` — ฟอร์ม「สนใจทรัพย์นี้」จะส่งข้อความเข้า Line ของคุณได้
- **Line Notify** ยุติบริการ **31 มี.ค. 2568** — ถ้ายังใช้อยู่ แนะนำเปลี่ยนไปใช้ Messaging API ตาม `.env.example`

---

## สรุปสั้นๆ

- **รันในเครื่อง ใช้แบบธุรกิจเต็มรูปแบบได้แล้ว:** สมาชิกจริง (DB + session), ฝากขาย/เช่าบันทึกลง DB, อัปโหลดรูปได้, หลังบ้านจัดการได้
- **ก่อนใช้:** ตั้งค่า `.env` (ADMIN_PASSWORD, SESSION_SECRET; ถ้าต้องการรับแจ้งเตือนใน Line ใช้ LINE_CHANNEL_ACCESS_TOKEN + LINE_AGENT_USER_ID) แล้วรัน `npx prisma db push` + `npm run db:seed`
- **ถ้า Deploy ขึ้นเซิร์ฟเวอร์:** ใช้ DB โฮสต์ (PostgreSQL) แทน SQLite และพิจารณาเก็บรูปใน Cloud Storage (S3/Cloudinary) แทนโฟลเดอร์ local
