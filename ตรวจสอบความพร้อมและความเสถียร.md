# ตรวจสอบความพร้อมและความเสถียร — เว็บอสังหามืออาชีพ

สรุปจุดที่ตรวจและปรับแล้วให้ใช้งานได้จริงและเสถียร

---

## หน้าแอดมิน — ลงลิสใหม่ / แก้ไขรายการ

- **ข้อมูลช่องทางติดต่อเจ้าของทรัพย์** — มีส่วนกรอกชัดเจนแล้ว:
  - ชื่อเจ้าของ/ผู้ติดต่อ *
  - เบอร์โทร *
  - อีเมล *
  - Line ID เจ้าของ (ไม่บังคับ) — ใส่ @username หรือเบอร์ที่ใช้กับ Line
  - WhatsApp เจ้าของ (ไม่บังคับ) — เบอร์พร้อม country code
- ข้อความอธิบาย: บันทึกข้อมูลติดต่อเจ้าของทรัพย์ไว้ใช้ติดต่อภายใน — ลูกค้าที่สนใจจะติดต่อผ่านช่องทางของเรา

---

## ความเสถียรและ validation

- **API บันทึก/แก้ไขรายการ (แอดมิน)**  
  ตรวจและส่งข้อความผิดพลาดชัดเจนเมื่อ:
  - ไม่กรอกหัวข้อ
  - ไม่กรอกข้อมูลติดต่อเจ้าของ (ชื่อ, เบอร์โทร, อีเมล)
  - ราคา/พื้นที่ต่ำกว่า 0
- **การอ่านข้อมูลจาก DB**  
  ใช้ `parseJsonArray` ใน property-db เพื่อกัน JSON เสีย (images, features) ไม่ให้แอป crash
- **การบันทึกรายการ**  
  ตรวจว่า `images` และ `features` เป็น array ก่อนส่งเข้า DB

---

## ฐานข้อมูล (Supabase)

- **ถ้าสร้างตารางใหม่**  
  ใช้ไฟล์ `supabase-create-tables.sql` (มีคอลัมน์ contactLine, contactWhatsapp แล้ว)
- **ถ้ามีตารางอยู่แล้ว**  
  รัน `supabase-add-owner-contact-columns.sql` ใน SQL Editor เพื่อเพิ่มคอลัมน์ช่องทางติดต่อเจ้าของ

---

## จุดที่ควรตรวจก่อนใช้งานจริง

1. **Vercel** — ตั้งค่า `DATABASE_URL` (แบบ Pooler + `pgbouncer=true`) และ Redeploy
2. **หลังบ้าน** — ตั้งค่าเว็บ (ข้อมูลติดต่อบริษัท) และทดสอบลงลิส/แก้ไขรายการ
3. **Telegram/Line** — ตั้งค่าแจ้งเตือนเมื่อลูกค้ากด "สนใจทรัพย์นี้"
4. **รหัสผ่าน** — เปลี่ยน `ADMIN_PASSWORD` และ `SESSION_SECRET` ก่อนใช้จริง
