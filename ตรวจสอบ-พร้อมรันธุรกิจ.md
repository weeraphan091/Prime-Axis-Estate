# ตรวจสอบความเรียบร้อย · ความปลอดภัย · พร้อมรันธุรกิจจริง

สรุปการตรวจสอบและสิ่งที่แก้ไขแล้ว

---

## 1. ความปลอดภัย (Security)

### ผ่านแล้ว
| หัวข้อ | สถานะ |
|--------|--------|
| **ข้อมูลเจ้าของทรัพย์ไม่ส่งให้ลูกค้า** | ✅ API รายการ (GET /api/properties, GET /api/properties/[id]) ไม่ส่ง contactName, contactPhone, contactEmail, contactLine, contactWhatsapp, userId ให้สาธารณะ — แก้แล้ว |
| **ข้อมูลเจ้าของส่งเข้า Bot เท่านั้น** | ✅ ฟอร์มสนใจทรัพย์ ส่งข้อมูลเจ้าของเข้า Telegram/Line Bot เท่านั้น ไม่ return กลับไปที่หน้าบ้าน |
| **รหัสผ่าน** | ✅ เก็บแฮชด้วย bcrypt (10 rounds), ไม่เก็บรหัสต้นทาง |
| **Session หลังบ้าน** | ✅ JWT ลงนามด้วย secret, cookie httpOnly + secure (production), บังคับ ADMIN_SESSION_SECRET/SESSION_SECRET อย่างน้อย 32 ตัวใน production |
| **สิทธิ์หลังบ้าน** | ✅ ทุกหน้า/API ตรวจสอบ hasAdminSession หรือ canAccessSettings/canManageAdminUsers ตาม role |
| **รายการของสมาชิก** | ✅ /api/my-listings กรอง where userId = session.userId เท่านั้น |
| **SQL Injection** | ✅ ใช้ Prisma (parameterized) |
| **XSS** | ✅ dangerouslySetInnerHTML ใช้เฉพาะ JSON-LD (JSON.stringify) ไม่มี user input |
| **robots.txt** | ✅ กัน /admin และ /api ไม่ให้ search engine crawl |

### ที่คุณต้องทำ
- ตั้ง **ADMIN_SESSION_SECRET** (หรือ SESSION_SECRET) บน Vercel **อย่างน้อย 32 ตัวอักษร** แล้ว Redeploy
- อย่า commit ไฟล์ `.env` หรือค่า secret ขึ้น Git

---

## 2. ความถูกต้อง (Correctness)

### ผ่านแล้ว
| หัวข้อ | สถานะ |
|--------|--------|
| **Validation ฟอร์ม** | ✅ สมัครสมาชิก/ล็อกอิน ตรวจอีเมล รหัสผ่าน (อย่างน้อย 6 ตัว), แอดมินสร้างบัญชีตรวจอีเมลไม่ซ้ำ รหัส 6 ตัวขึ้นไป |
| **API ที่ต้องล็อกอิน** | ✅ ฝากขาย, อัปโหลดรูป, รายการของฉัน ตรวจ session |
| **API เฉพาะแอดมิน** | ✅ จัดการรายการ/ลีด/พนักงาน/สมาชิก/ตั้งค่า/บัญชีผู้ใช้/Export ตรวจ hasAdminSession หรือ canAccessSettings/canManageAdminUsers |
| **Error handling** | ✅ API มี try/catch, คืน error message ที่เหมาะสม |

### ตรวจก่อนรันจริง
- รัน migration SQL ใน Supabase ให้ครบ (AdminUser, viewCount/userId ใน Property ฯลฯ) ตามไฟล์ในโฟลเดอร์
- ตั้งค่า **DATABASE_URL**, **SESSION_SECRET**, **ADMIN_SESSION_SECRET** (หรือ SESSION_SECRET 32+ ตัว) บน Vercel
- ตั้ง **TELEGRAM_BOT_TOKEN** + **TELEGRAM_CHAT_ID** (หรือ Line) ถ้าต้องการรับแจ้งเตือนฟอร์มสนใจทรัพย์

---

## 3. พร้อมรันธุรกิจ (Business readiness)

### ฟีเจอร์หลัก
| ฟีเจอร์ | สถานะ |
|---------|--------|
| หน้าแรก / ค้นหาทรัพย์ / รายละเอียดรายการ | ✅ |
| ฟอร์มสนใจทรัพย์ → แจ้งเตือน Bot + บันทึกลีด | ✅ |
| ฝากขาย/เช่า (สมาชิก) + รายการของฉัน + สถิติดู/สนใจ | ✅ |
| หลังบ้าน: แดชบอร์ด, จัดการรายการ, ลีด, พนักงาน, สมาชิก, ตั้งค่า, บัญชีผู้ใช้ | ✅ |
| ล็อกอินแอดมินด้วยอีเมล+รหัส, สิทธิ์ admin/staff | ✅ |
| SEO: meta, sitemap, robots, JSON-LD | ✅ |
| Supabase Storage สำหรับรูป (หรือ fallback data URL) | ✅ |
| Export CSV รายการ/ลีด | ✅ |

### แนะนำก่อนเปิดใช้จริง
1. **เปลี่ยนรหัสแอดมินแรก** หลังล็อกอินครั้งแรก (ถ้ามีฟีเจอร์เปลี่ยนรหัส) หรือใช้รหัสที่แข็งแรงตั้งแต่ต้น
2. **ตั้งค่าโดเมน + SSL** บน Vercel (ถ้ายังใช้ *.vercel.app อยู่)
3. **ใส่ NEXT_PUBLIC_SITE_URL** ใน Vercel ให้ตรงกับโดเมนจริง (เพื่อ canonical, sitemap, Open Graph)
4. **สำรองฐานข้อมูล** เป็นระยะ (Supabase มีจุดคืนค่าได้)
5. **ตรวจหน้า contact / ข้อมูลติดต่อ** ในหลังบ้านว่าเบอร์/Line/อีเมลถูกต้อง

---

## 4. สรุป

- **ความปลอดภัย:** ข้อมูลเจ้าของทรัพย์ไม่รั่วไปที่ลูกค้า, session และสิทธิ์หลังบ้านตรวจครบ, บังคับ secret ใน production
- **ความถูกต้อง:** validation และการตรวจสิทธิ์ API ตรงกับที่ออกแบบ
- **รันธุรกิจจริง:** ฟีเจอร์ครบสำหรับนายหน้า/ฝากขาย/ลีด/แอดมิน; แนะนำตั้งค่า env, โดเมน และรหัสผ่านให้เรียบร้อยก่อนเปิดใช้
