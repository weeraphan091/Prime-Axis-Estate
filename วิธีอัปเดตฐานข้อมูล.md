# วิธีอัปเดตฐานข้อมูล

มี **2 ทาง** เลือกทำอย่างใดอย่างหนึ่งก็ได้

---

## ตารางบัญชีแอดมิน (AdminUser) — เข้าหลังบ้านด้วยอีเมล+รหัสผ่าน

หลังบ้านใช้ **อีเมล + รหัสผ่าน** จากตาราง **AdminUser** (ไม่ใช้ ADMIN_PASSWORD ใน .env อีกต่อไป)

1. **สร้างตาราง:** รัน `npx prisma db push` หรือรันไฟล์ **`supabase-migration-admin-users.sql`** ใน Supabase SQL Editor
2. **สร้างบัญชีแอดมินคนแรก:** รัน `npm run db:seed`  
   - จะสร้างบัญชี **Kiranat56201@gmail.com** รหัสตามค่าใน .env ชื่อ `ADMIN_INITIAL_PASSWORD` (ถ้าไม่ใส่จะใช้ค่าเริ่มต้นที่ตั้งไว้)
3. เข้าหลังบ้านที่ `/admin/login` ด้วยอีเมลและรหัสนั้น

---

## ทางที่ 1: รัน SQL ใน Supabase (แนะนำ)

1. เปิด Supabase → โปรเจกต์ของคุณ → **SQL Editor**
2. เปิดไฟล์ในโฟลเดอร์โปรเจกต์ เช่น **`supabase-migration-rent-lease.sql`** หรือ **`supabase-migration-admin-users.sql`**
3. คัดลอกเนื้อหาทั้งหมด วางใน SQL Editor แล้วกด **Run**

เสร็จแล้ว = คอลัมน์/ตารางใหม่ถูกเพิ่ม

---

## ทางที่ 2: รันคำสั่ง Prisma จากเครื่องคุณ

ใช้เมื่อคุณต้องการให้ Prisma ส่งคำสั่งอัปเดตไปที่ฐานข้อมูลโดยตรง (ต้องมี `DATABASE_URL` ในไฟล์ `.env` แล้ว)

**วิธีรัน:**

1. เปิดโฟลเดอร์โปรเจกต์ (โฟลเดอร์ที่มีไฟล์ `package.json`)
2. ดับเบิลคลิกไฟล์ **`รัน-prisma-db-push.bat`**
3. รอจนขึ้นข้อความสำเร็จ แล้วกดปุ่มใดก็ได้เพื่อปิดหน้าต่าง

หรือถ้าจะรันเองใน Command Prompt / PowerShell:

```text
cd โฟลเดอร์อสังหา
npx prisma db push
```

---

## สรุป

- **ใช้ Supabase อยู่แล้ว** → แนะนำให้ใช้ **ทางที่ 1** (รัน SQL ใน Supabase)
- **ทางที่ 2** = ทำจากเครื่องคุณ โดย Prisma จะเชื่อมกับ DB ตามค่าใน `.env` แล้วอัปเดตตารางให้ตรงกับ `schema.prisma`

ทำอย่างใดอย่างหนึ่งแล้ว ไม่ต้องทำซ้ำอีก
